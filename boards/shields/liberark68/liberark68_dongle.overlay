/*
 * LiberArk68 - DONGLE (third Xiao nRF52840 + Prospector adapter).
 * Pure BLE central — no local kscan. Hosts the keymap, matrix transform,
 * and physical layout so ZMK Studio can edit them.
 */

#include "liberark68.dtsi"
