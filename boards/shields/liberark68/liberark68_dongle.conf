# LiberArk68 - DONGLE (central + ZMK Studio host)

# Studio
CONFIG_ZMK_STUDIO=y
CONFIG_ZMK_STUDIO_LOCKING=n

# USB HID output (so the dongle is the device that talks to your computer)
CONFIG_ZMK_USB=y
CONFIG_ZMK_HID_REPORT_TYPE_NKRO=y

# Pointing/mouse HID — required for the right-bottom encoder's scroll-wheel
CONFIG_ZMK_POINTING=y

# Battery widgets on the Prospector display want to know peripheral count
CONFIG_ZMK_BATTERY_REPORTING=y

# Prospector status screen — pick exactly one of:
# CONFIG_PROSPECTOR_STATUS_SCREEN_RADII=y
CONFIG_PROSPECTOR_STATUS_SCREEN_FIELD=y
# CONFIG_PROSPECTOR_STATUS_SCREEN_OPERATOR=y

# Optional Prospector tuning
CONFIG_PROSPECTOR_USE_AMBIENT_LIGHT_SENSOR=n
CONFIG_PROSPECTOR_FIXED_BRIGHTNESS=80

# Bump LVGL buffer if you hit RAM overflow
# CONFIG_LV_Z_VDB_SIZE=25
