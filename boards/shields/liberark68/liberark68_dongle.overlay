/*
 * LiberArk68 - DONGLE (Xiao nRF52840 + Prospector adapter, ST7789).
 * Pure BLE central — no local kscan. Hosts the keymap, matrix transform,
 * and physical layout so ZMK Studio can edit them. The display is wired up
 * by the prospector_adapter shield.
 *
 * DIAGNOSTIC: the fader input-split receivers + hid-io listeners are
 * temporarily removed while we isolate a USB regression — enabling hid-io's
 * second USB HID interface stopped keypresses (and the joystick) reaching the
 * PC. Restore the receivers/listeners from git (commit e84e176) once the
 * keyboard + hid-io + Studio USB coexistence is sorted.
 */

#include "liberark68.dtsi"
